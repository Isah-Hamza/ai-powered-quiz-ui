export const baseUrl = "http://localhost:5500";

export const apiEndpoints = {
  LOGIN: `${baseUrl}/auth/login`,
  REGISTER: `${baseUrl}/auth/register`,
  QUESTION: `${baseUrl}/questions`,
  USER: `${baseUrl}/auth/users`,
};
